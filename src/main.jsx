import React from 'react';
import ReactDOM from 'react-dom/client';
import PackageTracker from '../package-tracker.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PackageTracker />
  </React.StrictMode>
);
